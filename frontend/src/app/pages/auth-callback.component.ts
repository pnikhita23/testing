import { Component } from '@angular/core';

@Component({
  selector: 'app-auth-callback-component',
  template: ``,
})

/**
 * A blank callback page that users are redirected to after login so that the home page
 * does not "flash" before they are redirected to their actual destination.
 */
export class AuthCallbackComponent {}
